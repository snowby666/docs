---
title: "Get Model Breakdown"
openapi: "GET /user/models"
---

Per-model usage: `total_consumption`, `models` map (`requests`, `input_tokens`, `output_tokens`, `total_cost`, `owned_by`), and `last_updated`.
